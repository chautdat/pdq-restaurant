name: CD - Deploy

on:
  workflow_dispatch:

jobs:
  deploy-info:
    name: Deployment Information
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy Info
        run: |
          echo "=========================================="
          echo "CD - Deploy Workflow"
          echo "=========================================="
          echo ""
          echo "✅ Manual deployment mode"
          echo ""
          echo "This is for production deployment."
          echo "Currently disabled for development."
          echo ""
          echo "=========================================="
