spring:
  datasource:
    url: jdbc:mysql://mysql:3306/pdq_restaurant?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: pdq_user
    password: pdq_password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
